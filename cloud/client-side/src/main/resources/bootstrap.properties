#spring.profiles.active=<profile>
#spring.application.name=<spring.application.name>-<profile>.<yml/properties>
#The files are located via spring.cloud.config.server.git.uri in the server side application.properties file
spring.profiles.active=default
spring.application.name=client-side
spring.cloud.config.uri=http://localhost:8001

server.port=8002
